# Domain model (repo-local alias)

This directory is a **thin alias** for the **ubiquitous-language** workflow (DDD glossary). Canonical skill body: **`.cursor/skills/ubiquitous-language/SKILL.md`** (vendored from `skills/deprecated/ubiquitous-language/`).

There is no separate upstream skill named `domain-model` in mattpocock/skills.
