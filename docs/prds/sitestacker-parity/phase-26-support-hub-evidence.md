# Phase26 — Specification evidence and release boundary

Prepared on 15 September 2026 from all forty ratified Phase26 decisions, including D27-C, D29-X01 and all incorporated corrections. The user explicitly authorized `/to-spec` publication and confirmed the testing approach. No product decision was reopened.

## Source and authority

Core source and live develop were refreshed at 2026-09-15T09:52:04.366799+00:00; both identify `7abd2c11ffd4ed70c6775c4fd6f51c996e4350dd`. The actual source, CONTEXT map/glossary, applicable platform and feature ADRs, OpenSpec, API/database/Storage paths and tests informed the implementation contract. Current mock/demo/legacy behavior is recorded as an implementation gap where it conflicts with the accepted contract. Open predecessor work is a dependency qualification, not proof of shipped behavior.

The complete published trace records every one of 591 captured source files and 5,815 Markdown headings with a disposition, reason and source hash. Its 6,557 mapping rows include repeated targets and file-only evidence; this is not a count of unique requirements. All source files matched the captured hashes. The 217 user stories and 2,210 detailed requirement definitions retain their complete additional acceptance and operational-control suites.

Native Asym Postgres CRM authority wins over stale Twenty-backed specification language. The existing native-CRM retirement change owns that correction. Support consumes authorized shared context and owning-domain commands; it does not add synchronization or another CRM. Platform ADR identifiers and Support feature ADR identifiers are distinct namespaces.

## Confirmed testing contract

The founder confirmed one main acceptance harness through real authenticated Support/API commands and authorized projections plus canonical intake/job boundaries, using disposable local Supabase. Focused real-role database/grants/RLS/Storage/concurrency/migration proof and complete authenticated Playwright journeys share that backend. Existing tooling and established seams are retained; ordinary unit tests remain offline. Deterministic external-I/O substitutes do not replace separately qualified real-provider evidence. Accessibility, performance, migration, operational and intended-user proof remain required exactly where the normative contract specifies them.

## Completed documentary checks

- 28 structural, exact-reference, source-integrity, mirror and OpenSpec checks passed.
- All 217 stories and 717 main acceptance outcomes round-trip into the formal OpenSpec scenarios; its shorter scenarios do not replace the full detailed suites.
- Strict scoped OpenSpec validation passed with zero issues. All 237 future implementation/qualification tasks are unchecked.
- Independent scope-specific reviews covered the complete source classification, data/authorization/lifecycle and Email Studio seams, UI/UX/terminology and final generation. Exact hashes bind reviewed artifacts. These reviews combine source synthesis, mechanical coverage and targeted semantic checks; they do not claim a word-by-word formal equivalence proof of the historical archive.
- The publication process passed 21 offline integrity/recovery checks. Every public part must subsequently be read back in full and matched to its exact reviewed manifest before this issue is marked ready.

## What has not been performed

This publication performs no feature implementation, schema migration, production change, provider qualification, live inbox/DNS connection, real message, participant outreach or implementation-ticket graph. It does not claim that application tests, live database permissions, real browser behavior, accessibility/usability studies or production capacity passed. Those are future release obligations with the exact scope and gates in the spec.

Local OpenSpec and formal-document mirrors are validated planning artifacts. This is an issue-tracker publication, not a claim that a source commit or PR was published. Existing truthful Git attribution constraints are preserved.

## Publication mechanics

The repository-configured tracker is GitHub. The final spec uses the current labels `type:feature`, `status:ready` and `complexity:hard`. Ordered volumes on the same issue make all normative content available without relying on local files. Content hashes and a persistent intent prevent blind duplicate creation; incomplete publication remains non-ready until full readback succeeds.

Sequential mutation and conservative bounded parts follow the [GitHub REST API best practices](https://docs.github.com/en/rest/using-the-rest-api/best-practices-for-using-the-rest-api). The [issues API](https://docs.github.com/en/rest/issues/issues#create-an-issue) and [issue-comment API](https://docs.github.com/en/rest/issues/comments#create-an-issue-comment) are the publication surfaces. These API references govern publication mechanics, not Support product behavior or authority.
