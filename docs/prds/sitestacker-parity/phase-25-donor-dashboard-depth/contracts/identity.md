# Identity, access and personal-contact contract

## IC01 — Ownership, scope and source boundaries

Phase25 SHALL provide the ratified personal/represented access, email-first authentication, selected related email changes, ordinary personal-contact Name/Phone and one current mailing address. Supabase Auth owns native principals, identities, credentials, sessions and token issuance. Phase4 owns verified claim/link intent and possession policy; Phase9 owns CRM persons, Party relationships and admitted contact facts; Phase12 owns the sole current authorization decision; Phases17 and6 own prepared communications and dispatch. Phases7,13,16,18 and19 retain legal donor, money, commitment, document and fulfillment authority. One system SHALL NOT silently substitute another's fact.

An Auth principal, person Party, donor record, Active Tenant Assignment, represented Party, Commitment Party, financial authorizer, provider Customer and document recipient remain distinct. Changing an address/name, choosing a sign-in method or switching a view creates no gift, financial authorization, claim, account merge, consent, newsletter enrollment or historical-document rewrite. Asym Postgres remains CRM truth; no retired CRM dependency, generic contact-normalization platform, per-provider donor store, identity graph, new broker or alternate JWT issuer is introduced. Shared Auth/business clients and callbacks SHALL be reused, with thin app entry points and one authoritative owner mutation per fact.

The narrowly stated source amendments in this contract are implementation requirements, not evidence of existing storage or permission. Explicitly reconcile the earlier overloaded claim/contact verification, optional-social and provider-email trust assumptions, personal-contact field scope, postal audit/custody exceptions and older fulfillment/source wording before activating their consumers. Existing scoped restrictions survive; unknown ownership or proof SHALL NOT be guessed during migration.

## IC02 — Current context and representation

Every entry and protected operation SHALL resolve the verified organization host and the appropriate existing Phase12 Tenant Authorization Context. Authenticated humans use one validated Active Tenant Assignment. A source or record grant constrains resource, subject and purpose within that context; it is not a fifth context or broader list grant. Public readers and exact guest/document handoffs retain their existing owner admission contracts. Missing/failed membership resolution SHALL NOT become a profile-role, staff, superadmin or demo-Tenant fallback. The application and coarse RLS SHALL use the same trusted Tenant source, while Phase12 alone makes fine-grained capability/field/purpose decisions. Raw API, database, storage, realtime and privileged paths SHALL preserve that boundary.

On neutral entry, personal giving opens first when present. A visible, clearly labelled conditional switch exposes only separately admitted giving the person helps manage, with the active represented name on relevant pages and consequential reviews. With only personal access there is no chooser. With one represented context and no personal donor context, enter it directly. With several represented contexts and no personal context, present a clear initial choice. Do not fabricate a financial donor record, gift or personal claim to satisfy a snapshot. A legitimate person/contact record is created only through its authorized identity/CRM flow, without requiring a donation.

A valid targeted link, provider return or accepted task retains its exact authorized subject rather than resetting to personal giving. A document-only or commitment-only grant does not expose a whole organization's profile, wallet or history. Context names, options and counts require independent admission. Switching is navigation, not impersonation; it changes no login, Tenant, Site/brand, consent, default payment method, financial instruction or document ownership. Accepted work stays bound to its original context. Ordinary switching needs no confirmation; warn only about genuinely unsaved work that would be lost. No cross-device last-used-context preference is added.

Personal Profile, Updates, account security and personal communication choices remain attached to the human rather than the represented financial Party. Household membership, job title, shared email, recognition or matching names/addresses grant no access. Revoke and fence obsolete responses so old data never appears beneath a newly selected heading. Revocation yields a truthful unavailable state and safe navigation, never silent retargeting of an old action.

## IC03 — Email-first entry, proof and safe return

Email SHALL remain the primary sign-in identifier. The first qualified sign-in email contains a prominent safe link and a secondary code; either redeems the same issuance once. Explain that one is enough. Public attribution/claim outcomes remain enumeration-safe, claiming remains optional, and source-public content stays public when permitted. No automatic sign-up/claim from provider metadata or donor-row match is allowed. A clean unclaimed record still follows Phase4's separately governed fresh-possession rule; this contract promises no detection of unknowable mailbox recycling.

The source SHALL bind the issuance to exact normalized mailbox, purpose, request/revision, expiry, replacement and actor context. Link and code cannot leave a reusable sibling proof after either completes. Code length follows the qualified producer contract; treat it as one logical input supporting paste, one-time-code autofill, leading zeros and normal keyboard editing. A code used on one device is not reusable on another and does not transfer a session automatically. Wrong, expired, consumed, replaced and rate-limited proofs have concise qualified recovery. Governed resend follows the actual current issuance; rendering, polling, blur and page loading send no message or OAuth request.

The emailed protected-action landing SHALL use the existing selector-plus-fragment protocol: a nonsecret opaque selector in the HTTP URL and an independent 256-bit protected verifier in the fragment. GET/HEAD, previews and scanners establish no session, consume no proof, expose no protected fact and perform no business action. The minimal qualified first-party landing script removes the fragment from visible history and prepares, but never submits, the same-origin form. Missing/stripped fragments have no selector/query/cookie fallback. Only deliberate protected same-origin POST validates the exact pair and may establish the bounded non-authorizing landing session and permitted next step. Codes likewise reach explicit owner verification. Credentials SHALL NOT enter ordinary URL history, logs, analytics, previews or persistent browser storage.

An ordinary OAuth callback is different: its valid protocol GET may exchange the code after state/PKCE/nonce and exact request validation. Do not apply emailed-scanner behavior indiscriminately to that callback. Browser and server SSR clients remain distinct; propagate refreshed cookies to request and response. Signature-valid identity is not current permission; session payload alone is insufficient authorization. Private/Set-Cookie responses SHALL NOT be shared-cached or prefetched as another user.

After qualified authentication, reauthorize and return to the exact intended Tenant/task/subject before using a neutral-home fallback. No authority is accepted from arbitrary return paths, host headers, provider metadata or Site slugs. Lost/retired hosts cannot redirect credentials to another Tenant or unapproved central host. Invalid/revoked targets receive the safe current outcome, not another donor's record.

## IC04 — Native social identity safety and unresolved G01

Google, Apple and Facebook remain required optional donor-entry scope, each exposed only when independently qualified and offered. Supabase adapter support does not establish safe shared-principal admission. Before an affected route activates, the shared Auth owner SHALL qualify an officially supported control in the exact target deployment that prevents an unqualified new email-matching provider assertion from attaching an authenticator or obtaining usable native credentials for an existing principal before the ratified current-account/possession policy is satisfied. Exact sign-in by an already-bound stable provider subject is distinct from attaching a new identity by email.

G01 remains unresolved. Current public documentation, released Auth source and Management API inspection did not establish the supported selective native control. The creation hook does not govern existing-user linking; PKCE identity attachment can commit before code exchange. Generic OAuth token-hook input, provider inventory, verified-email boolean, user metadata, a manual-link-only UI, disabled sign-up, hidden button, post-link Core check or coarse RLS does not demonstrate that guarantee. Experimental linking-domain isolation and undocumented provider remapping are not approved permanent solutions. This is a conditional architectural blocker, not an observed live compromise or proof that the vendor can offer no supported arrangement.

Required qualification SHALL identify the supported setting/hook/API, trustworthy current provider/subject/possession inputs, exact transaction point, native authorize/ID-token/manual-link/code-exchange/refresh/credential-management coverage, and link/unlink/recovery outcomes. It SHALL address Google Gmail, verified Workspace and nonauthoritative third-party mailbox distinctions, changed/absent email, Facebook returned email, Apple real/relay identity and collisions. Stable signed provider subject is identity evidence; mutable email, display name or relay suffix is not a substitute. If no supported native solution can meet the contract, an explicit authentication-architecture or release-scope decision is required. Do not silently adopt a broker/fork, drop a requested provider or call email-only delivery completion of the social scope. No production enablement or implementation-ready social claim is permitted while this gate is unresolved.

## IC05 — Provider applications, branding and account lifecycle

Use platform-operated provider applications separated by environment, exact registered callbacks and identity-only scopes. Tenant controls choose offered qualified entry methods; they SHALL NOT imply that a shared principal is prohibited from having authenticated with a provider elsewhere. An actual method/assurance restriction requires its own current Phase12 policy. No per-Tenant Auth project, BYO-provider configuration matrix, automatic One Tap, mandatory popup, friends/contacts/content/social-post access, enterprise SSO, passkey enrollment or dummy native application is added.

Asym-controlled login, recovery, email and portal retain the organization brand and verified host. Provider-owned consent screens may display their provider and the qualified shared Asym application identity under the narrow branding exception. Explain Asym's role briefly before leaving. Use official marks/button treatments within shared Maia; mandatory provider pixels need not become Zinc. Email stays primary; qualified social controls have full labels, similar weight and stable Google/Apple/Facebook order, stacked comfortably on small screens. Normal web entry uses full browser redirect. Unsupported embedded-browser situations have a clear supported-browser continuation and usable email path.

Connecting a method proves the current account and new provider identity through qualified native linking. Preserve the exact principal; an identity attached elsewhere is a conflict, never automatic transfer, merge or deletion. Disconnecting requires a proven usable remaining sign-in/recovery route, not merely two identity rows. Explain account-wide effects without listing other Tenants. Any potential global email recomputation or session consequence must satisfy the related email/security contract before success is claimed. Link, unlink, signout and account closure remain distinct commands. Unknown effects reconcile the original operation rather than issuing fresh-key repeats or fictional rollback.

Apple qualification SHALL establish actual eligible app/team/Services ID/domain association, correct callback/audience, usable real/relay subject, exact sender registration/authentication and capacity, secret rotation and lifecycle event handling. Relay privacy is respected; do not demand the underlying address, identify relay solely by an old suffix or overwrite contact/legal facts. Qualify both existing and new relay-domain behavior. Apple's documented organization capacity at this specification checkpoint is 100 email sources; qualification must record and enforce the actual supported active-source limit before Tenant admission, rather than promise unlimited coverage or use a central-sender fallback. The generated web client secret expires within six months and differs from its signing key; the platform operator owns secure replacement/config propagation. Missing name cannot force profile completion. Qualified revocation/forwarding/account events reach existing identity/contactability/privacy owners, never direct cancellation of giving or deletion of retained financial records.

Facebook qualification SHALL use the actual app mode, minimal public-profile/email permission, adopted privacy/deletion/deauthorization contract and an ordinary non-role account. Development/test-role success is insufficient; do not invent universal review, verification or renewal requirements. Missing/denied email, cancellation and provider failure return to the same safe Tenant entry with email fallback, without fabricated addresses, repeated forced consent or technical error pages. Do not enable email-optional principals as a cosmetic fix. Google/Apple/Facebook app/team/domain/subject changes are governed credential migrations, not harmless branding changes. Each provider's qualifying configuration, capacity, current requirements and recovery are recorded before activation.

## IC06 — Selected related email changes and stable claim proof

Present actual Sign-in email and My contact email with this organization as distinct owner reads, including current/pending/unavailable states. Never use one as the other's fallback. The targeted entry selects only its named use; the other permitted personal-contact/sign-in use requires deliberate selection. A generic entry shows both permitted uses inline without another chooser page. Equal current strings do not select Both. Permit different current addresses; enter the proposed address once and review each selected old-to-new effect. Other Tenants are not enumerated, and represented giving grants no personal-contact edit authority.

Normalize and validate exactly one supported mailbox consistently for comparison, uniqueness, proof, provider requests and contact apply. Preserve supported display form; do not infer dot/plus aliases, extract a header list as an identity address or silently alter IDN/Unicode meaning. Invalid input remains editable. A source-proved no-op creates no verification message or false change; it does not cancel a different pending request.

An established principal/profile-to-Party/donor claim and its accepted historical proof SHALL remain separate from the current contact-email revision and its verification. A changed address inherits no old verified flag; clearing current contact proof cannot erase established claim/access evidence. Fresh possession cannot overwrite an already-bound person's claim or select/merge another account. Known retired/conflicting/reassigned/ambiguous evidence uses the existing identity exception owner; no oldest-match or shared-email guess. Existing legal donor, financial authorization and frozen records remain unchanged.

Before the first effect, persist immutable accepted selected uses, trusted principal/actor, launch Tenant/environment/host, exact personal contact/Party, expected identity/contact revisions, proposed normalized address, proof/operation correlations and qualified expiry. At least one eligible use is required; at most one sign-in and one contact step belong to this two-use request. The request owns intent and progress, not duplicated email truth. Each owner admits its own command and durable result. Known unready selected lanes prevent a misleading Both start; a separately qualified contact-only lane may remain available. No general workflow engine, financial replacement parent or claiming command substitutes for this task.

## IC07 — Email proof, request lifecycle and recovery

The ordinary email-only sign-in change requires distinct current-inbox authorization and new-inbox possession for the exact request. Reuse that current proof as fresh authorization rather than requiring a redundant third sign-in challenge; honor existing stronger-authentication/managed-identity rules without surprise enrollment. For the ordinary qualified Supabase lane, disable email autoconfirm and enable secure email change, then prove the intended deployment actually requires both exact proofs; a single flag or HTTP success is not proof. Either proof order is valid. Present progress and the remaining action without claiming completion from a provider event name, callback argument or accepted request.

A selected contact step applies only through its current authorized source revision and exact-purpose proof. Reuse new-inbox proof only through a qualified same-request exact-address/Party/purpose certificate. Otherwise request the necessary contact proof with a clear explanation rather than weakening it. Verification can complete the still-valid accepted instruction without redundant final Save or retyping.

Draft, accepted/pending proof, applying/reconciling, completed, incomplete with a known action, indeterminate, expired and superseded SHALL remain distinguishable per step. Before initiation Cancel discards only the draft. After initiation Finish later leaves valid pending authority until qualified expiry; leaving does not cancel sent proof. A different address requires proved successor/supersession, invalidates old proofs and resets required progress. Same-address resend, expiry, cooldown and recovery cancellation must follow their independently qualified owner semantics. An unchanged-current request, password reset, admin reassignment or raw Auth-table cleanup is not Cancel.

Serialize global sign-in transitions against the shared principal's monotonic revision and contact writes against the exact scoped revision. Email A→B→A is not revision equality. Same operation/same content reconciles; changed content conflicts. Accepted scope is immutable, late predecessor events cannot revive it, and changing back after completion is a new request. Never hold a database transaction while a donor checks an inbox. Preserve completed steps when another fails; do not reverse Auth to compensate for contact failure. Unknown effects read back the original operation and show Checking the change, not blind retry or failure. Lost-old-inbox and suspected requests use existing qualified security/recovery with minimal safe references, no unverified staff relink, forced new account or new helpdesk product.

## IC08 — Shared credential security and completing-device session

A shared email/method change SHALL reach every affected existing Tenant assignment through the existing Phase12 identity/epoch owner without disclosing other organizations or propagating their contacts. Preserve ordinary grants only where current rules permit; suspend/re-attest sensitive grants and recheck restricted named grants. Before external identity completion can outrun security reconciliation, Phase12 SHALL honor a narrow identity-transition/currentness fence at every supported writer and protected door. It is an input to the existing PDP, not a second permission engine. An incomplete attempt cannot permanently revoke ordinary access or manufacture grants when the fence releases.

After the sign-in email is confirmed, retain the exact newly verified session on the device where completion occurred and explicitly retire that principal's other existing sessions. Bind that session to the same principal/request; another person's signed-in browser cannot inherit the task. The original browser may sign in again and resume accepted work. Confirm retained access and cleanup before saying Other devices have been signed out. Email update, local logout, SDK no-error or an empty session snapshot does not prove cleanup or denial at all other services.

The native logout-others operation is time-relative rather than business-key-idempotent. Serialize bounded cleanup under the transition and retain the exact finishing session. While a cleanup call is unresolved, newly minted other sessions cannot obtain protected Core access on the obsolete/unresolved basis; do not claim the provider cannot mint them. Prove completion/readback and delayed-call ordering before marking security finished. After durable completion, no stale/retried worker invokes cleanup for that request or indefinitely revokes later authorized sign-ins. Contact-only change retains login/sessions and never enters this global path. No general session-management product is introduced.

## IC09 — Email-change messages, consent and disclosure limits

Use the signed source producer, immutable Phase17 preparation and sole Phase6 dispatch/evidence; no default Auth SMTP, direct sender, template readdressing or alternate-recipient fallback. Qualify each exact current/new verification, contact proof and security-completion purpose, recipient, proof mapping, occurrence, expiry, idempotency and recovery before lane activation. Required recipient/hash mapping is tested against the actual provider; it is not inferred from misleading field names.

The normal change sends two distinct inbox verification messages, then one qualified security-completion notice to the previously verified sign-in address with minimal content, masked new address and safe security-help destination. That is an explicit identity-owned occurrence; do not readdress a native current-address notice using an old-email variable. Preserve/reconcile any independently required current-address native notice. Add no discretionary duplicate success email. Contact-only sends its exact new-contact verification and shows the result in the portal. Both reuses new proof only when qualified, avoiding an unnecessary third verification message. Notice delivery failure does not roll back identity completion, relax suppression/security rules or retarget the message; preserve exact failed/unknown evidence and owner recovery.

Preserve authored communication On/Off choices and higher restrictions. Contact change is neither enrollment, unsubscribe, bounce clearing nor suppression removal. Future delivery uses exact Party/contact revision/purpose; no Tenant-plus-email first match. Frozen queued recipients/payload remain frozen; stale unstarted work is suppressed/expired or deliberately owner-reissued under a new occurrence. Already-admitted or uncertain sends reconcile honestly. Receipt quieting, Updates hiding and external missionary newsletters keep independent truth.

Public guest/claim setup retains uniform non-enumeration. Authenticated sign-in-email change may expose the provider's narrow credential-address-unavailable signal; this accepted assurance boundary supplies no donor/profile/Tenant membership details and cannot select or merge an identity. Actual reachable native and Core endpoints require abuse/rate qualification; hiding keys or only limiting the UI does not erase the signal. Email/code is not phishing-resistant MFA or a claim of higher assurance compliance.

## IC10 — Ordinary personal-contact Name and Phone

Provide one exact-Tenant, proved-person ordinary personal-contact Name and optional clearable Phone through the owning Phase9 subtype/contact service and current Phase12 field/purpose capability. This is an explicit finite source adoption/extension, not proof a canonical field already exists or that scope acceptance grants edit rights. Name is one unsplit Unicode value for this contact/display purpose; do not infer given/family parts, require a surname, reorder or transliterate names, or overwrite independent structured/legal values. The owning subtype service alone controls any intended Party display-name write-through; Party receives no inline contact columns. Missing source data does not force profile completion or block unrelated giving. The source field contract declares meaningful input and clearability; blank is never replaced silently by an Auth email or another purpose's value.

Phone preserves human input, leading plus and meaningful extensions. Derive normalized dialing only with sufficient region evidence, never automatically from display locale or mailing country. Use phone-appropriate input/autocomplete without numeric-only input or rigid masks. A contact number is not login/MFA/recovery enrollment, SMS consent, verified possession or a channel preference. Shared typed bounds/normalization validate complete input without silent truncation. Neither field changes Auth, another Tenant/person, represented Party, provider billing, issued/legal addressee or released missionary/public identity; future source-qualified document/public uses retain their own authority.

Validate all selected fields and permissions before any effect. One local Phase9 transaction co-commits admitted values, monotonic revision and required audit/activity. Use expected revisions and narrowly scoped change-record correlation for retry/readback; no reserved generic CRM command log or provider journal. Return durable acceptance separately from a later page refresh. Invalid second field or audit failure writes nothing; a response lost after commit reconciles without duplicate effect; stale/ABA updates cannot overwrite current data. Minimized audit carries field/revision/actor/purpose/result, not raw contact payloads. All reached REST, GraphQL, RPC, collection and fallback readers/writers SHALL converge; no alternate route may save a profile and then reject a missionary side effect, mirror independent values or make a clear reappear from an old fallback. Mailing-specific or financial retention clocks are not defaults for Name/Phone.

## IC11 — One optional mailing address and international editor

Show one current personal mailing contact within My details for the exact authorized person and organization, with independent read/add/edit/remove capabilities. Absence is valid and creates no incomplete-profile score, warning or prerequisite to digital giving. Other independently required checkout/billing/issuer facts remain their owner's requirements. This is not global login, household/org address, represented legal address, public missionary location, card billing or legal donor identity. No saved-address library, seasonal switching, current/default selector, grid, map, address score or verification badge is added; preserved staff alternatives are not automatic fallback choices.

Show full admitted postal lines without two-line clipping. Exact absence says No mailing address added with permitted Add; unavailable is a local truthful retry state, not an editable empty. Readable but not editable stays a summary with qualified help. Add/Edit replace only that summary with a single-column inline editor; stable page context and other settings remain. Prefill only current admitted values. A new value starts without inferred country; the saved country can be retained. Country/region is visible, localized, keyboard-searchable and explicitly stored, never inferred from IP/language/issuer/currency. Country changes preserve all draft content and show remapping for review, but submit only the selected reviewed representation.

Use shared Field/FieldGroup/Input/Textarea and the installed shared Base UI Select with proved keyboard typeahead/autofill. Do not assume an unimplemented shared Combobox; if the Select fails the task, add only the smallest shared country-control enhancement without changing primitive base. Use code-owned versioned country formats with only necessary local fields. Preserve scripts, spelling, units, leading-zero/alphanumeric postal codes, rural, care-of, PO box and military/diplomatic forms. Do not split house/street, strip accents, uppercase storage or require vendor lookup. An unqualified structured format opens manual Address as written for mail; the quiet Use address lines instead action stays in the same editor. Manual is one representation of the same address, not another current record. Consumers must explicitly admit structured/manual form, preserve manual ordering and never guess city/postcode or assume every mail-house can use it. Unsupported fulfillment is separate from successful contact saving.

The tagged value uses one closed qualified country/territory code and exactly one structured/manual representation. Manual permits eight ordered delivery lines up to 200 Unicode scalar values each. Structured street lines have the same eight-line capacity; locality, region and dependent locality each permit up to 200 scalars, postal code up to 64, and the total address-value payload is at most 16KiB UTF-8. These are product limits, not universal postal standards. Reject excess, controls/markup or invalid shape with exact errors, never truncate; preserve legitimate script shaping and safe mixed-direction display. Reveal extra lines on request rather than showing eight empty inputs. Care-of is a line, not legal-addressee editing. Autofill/paste/blur and suggestion selection do not save; any future lookup remains optional editable convenience, not identity, occupancy, deliverability or permission proof.

Save address is primary; Cancel is secondary; quiet Remove is inside Edit. Ordinary saves require no postal verification, routine reauthentication, reason, approval or extra review screen. Current session/authorization is mandatory. Clean Cancel closes immediately; only a dirty leave requires one discard choice. Validate on blur/submit, keep input, associate errors and offer a concise submit summary. Pending is announced once with stable Saving state; no optimistic saved address, confetti or toast-only completion. Accepted, no-op, superseded current result, unknown and conflict remain distinct. Return the currently admitted value or preserve accepted outcome without claiming an obsolete snapshot is current. Drafts stay in memory; no unattended offline write queue or private URL/localStorage/session replay. Lost response offers same-operation Check status/retry.

## IC12 — Postal source, atomicity and retry invariants

The personal-mailing source is one logical aggregate per Tenant/person Party/purpose, at most one current binding and a monotonic revision guard that survives Clear. Value revisions are immutable successors with trusted use-generation references. Proved same-delivery-point formatting successors retain a generation; a material replacement or Clear withdraws it, and a newly usable value gets a non-reusable generation. No general equivalence registry, address-string matching or ancestor scan is introduced.

One authorized transaction validates the whole value, compares expected current revision and commits binding/value successor or Clear, exact required withdrawal, source change evidence, CRM audit/activity and durable downstream event. A semantic no-op creates no revision, withdrawal, audit/activity change or postal review. Use scoped nonce uniqueness across Tenant/person/purpose and bind actor/payload server-side. Equivalent retry returns its original effect and separately current authorized revision; changed payload with the same nonce conflicts. Expired result evidence never resets the guard or allows an old revision to execute. No money-path idempotency protocol, provider gate or general command ledger is added.

Enforce composite same-Tenant/person/purpose keys, personal subtype, one current binding, current-to-own-value ownership, non-null accepted actor-kind/time/revision, valid tagged shape and immutable attribution. The caller's IDs/revision are target assertions, not trusted ownership, author, approval or success. No permitted update may reparent to another subject/purpose/Tenant. Prove effective grants/RLS old/new-row checks, execution roles, views/functions and service paths; restrict raw browser writes. Deletes cannot cascade into identity, financial/documents or fulfillment evidence. Native Auth safety remains independent.

Use the minimal authorized server projection through existing Query/approved hook boundaries and the shared typed form. Do not refresh all donor history to establish save success or force every TanStack library into one contact form. Index exact current lookup and run bounded resumable downstream review fanout; safety closes at commit rather than after a synchronous scan of every old piece.

## IC13 — Mailing withdrawal and fulfillment handoff

A material move or Remove SHALL withdraw future personal-mailing use of the superseded current generation, including still-usable formatting predecessors. Formatting A→B followed by material B→C or Clear must stop unadmitted work referencing either A or B. Without proof of same delivery point, handle a change as material. This does not invalidate the physical address for every person, withdraw general consent, retarget an independent purpose or require ancestor enumeration.

The source mutation and each final irreversible fulfillment admission serialize on the existing source safety guard. If withdrawal wins, no later handoff uses that old generation. If handoff already crossed, preserve submitted/possibly-submitted truth and reconcile. No transaction spans database and postal/provider I/O, and a late address save cannot promise prevention or recall. Phase19 freezes exact destination revision, use generation, selection reason and recipient authority. Current-primary changes stale affected preflight/hold unsubmitted work; a formerly specifically reviewed destination that depends on withdrawn use is no longer usable. A genuinely independent still-authorized destination is not silently withdrawn or switched. Safe succession uses current source proof/review and a new semantic occurrence; never choose new address, another address, email or print run automatically.

Prepared/downloaded/printed does not prove postal handoff. Revoke/hold controlled unsubmitted artifacts where supported, with existing staff containment for downloaded physical copies. Submitted/unknown work reconciles before a new attempt. Saving may succeed while existing fulfillment needs review; show only a relevant admitted note with actual status, no guessed deadline. Remove means no current value/fallback for this purpose; blank edit is not Remove. Confirm removal's exact effect and preserved records; it stops no giving, recurring arrangement, independent statement authorization or marketing choice. Later restoration requires a new authorized value/revision, never reactivation of the old withdrawn generation. Issued facts/bytes change only through source correction and document successor. Address save calls no email verification, resend or ad hoc sender.

## IC14 — Postal custody and history

Current raw value is retained only while the personal-mailing purpose is active. Superseded/cleared raw revisions leave the current editor immediately and the narrow contact owner's live raw store within 24 elapsed hours after retirement, subject to earlier applicable erasure and exact lawful hold. That raw-custody retirement does not revoke a separately frozen still-valid formatting-only fulfillment destination. Independent legal/document/fulfillment evidence retains its own purpose and schedule, not the contact timer.

The postal-only audit exception records safe before/after revision/state references, never full address diffs in generic audit/activity. These rows are immutable while retained and have a 365-day diagnostic ceiling after the change event, instead of an unconditional never-delete rule. Active monotonic guard and reference-needed withdrawal evidence remain for their separately defined safety purpose without raw value, reversible fingerprint or indefinite identity trail. When that purpose ends and no consumer references remain, remaining safety metadata enters the same 365-day disposal class from that terminal event. Evidence disposal cannot permit replay or reset the guard.

Apply exact owner floors/holds before activation where they conflict with ceilings, restricting use rather than inventing a universal legal period. Backup copies follow qualified copy-class expiry and restore suppression; live deletion is not immediate physical destruction from every backup. Protected location/country remains field/purpose restricted across API, logs, exports, caches, backup/restore and support. No address publication or external validation transmission by default. No longer raw-history/Undo/address-library purpose is implied.

## IC15 — Shared UX, currentness and observability

Use exact shared base-maia/Base UI, semantic Zinc tokens, existing typography/radii/motion and owner-controlled components. Compose persistent labels, errors/help, meaningful buttons and logical code input. Provider-mandated mark treatment is the only narrow external branding exception. Do not introduce app-local clients/themes, nested modal/wizard chains, mandatory unrelated name/photo/address completion, or large permanent steppers. Preserve readable full authorized email/address detail in the signed-in task and context-appropriate masking in security notices. Neutral missing/pending states do not become alarming red. Announce material transitions politely, not polling/countdown ticks; maintain focus except deliberate navigation or errors requiring attention.

Prove keyboard/screen-reader, paste/autofill, browser Back, narrow 320CSSpx reflow, 200% text/400% zoom, supported RTL/CJK/mixed scripts, reduced motion, mobile keyboard visibility and Core's 44px target preference with applicable WCAG minimums. Normal text contrast meets 4.5:1. Shared component names, current RTL configuration or axe alone do not certify the composition. Full address lines override unrelated two-line previews; no height flourish or automatic focus steal.

Caches and pending responses SHALL bind exact Tenant assignment, human, subject/purpose and source/access revisions. Dispose of affected Query/DB materializations and memory drafts on logout, context change, permission contraction and qualified same-principal identity transitions; cancellation alone is not enough if late results can publish. Server egress rechecks current authority; cached display is never an artifact or mutation grant. No public CDN or durable browser persistence of protected identity/contact results. Do not claim recall of bytes already delivered.

Maintain minimized cross-stage operation correlation for native effect, claim, source save, permission/session reconciliation, message and destination. Durable business/security evidence records actual actor/scope/revision/outcome separately from technical logs. No bearer tokens, raw hook bodies, provider tokens, full private URLs, contact payloads or sensitive session replay in ordinary telemetry. Sent/opened/read/presentation is not possession, consent, understanding or completion. Existing authorized identity/CRM/fulfillment support sees the exact failed/pending stage; staff cannot proxy donor proof, perform raw identity surgery or promise unsupported cancellation. Ordinary consent cancellation and code typo are not incidents.

## IC16 — Adoption, strongest testing seams and operating gates

Complete source constraints/permission and exact protocol qualification before new consumers. Reconcile all existing entry, callback, profile, donor, claiming, matching, consent, dispatch, GraphQL/RPC and direct-data writers/readers; retire incompatible mirrors/defaults rather than retaining alternate authority. Migrate claim/contact proof and current mailing choices only from trustworthy provenance, not equal email, current Auth verification, last activity, work-address JSON or provider observation. Unknown/ambiguous data stays unresolved without leaking another Party candidate. Qualify mixed-version readers/writers, pending operations, app/domain/subject continuity and source merge/revision conflicts. Kill new requests or an unqualified consumer while preserving safe readback/reconciliation, completed facts, current guards/withdrawals and independent historical access. Rollback cannot restore unsafe legacy writers, demo Tenant, default sender, old contact or canceled security work.

Use the confirmed highest test seam: complete donor journeys through real browser and shared public API, with focused public-handler unit tests for deterministic branches. Supplement with actual migrated PostgreSQL roles/grants/RLS/constraints/transaction/concurrent-session proof, and exact native Auth/provider endpoint qualification for G01. Existing mocked/source-text tests, migration compatibility Auth tables, demo bypass, catalog/source review and isolated historical observations SHALL NOT count as target Auth, PostgREST, browser, provider or release certification. Required fixtures must not silently skip. Reuse the existing harness families; add only missing acceptance coverage, no parallel framework or tests of private helper trivia.

Required proof covers every represented/personal/nonfinancial/record-only admission and revocation, both email-proof orders, scanner/replay/wrong-browser/host, every exposed native credential path, link/unlink/collision, retained finishing session/other-session cleanup including delayed calls, partial distributed effects, same-key changed content, ABA/supersession/expiry, consent/queued-mail races, actual local save/audit atomicity, all alternate profile writers, postal shape boundaries and handoff/withdrawal race orderings, independent/frozen destinations, 24h/365d custody and restore, no unintended money/document/public effects, accessible complete jobs and informed donor comprehension. Critical account confusion, unauthorized effect or inaccessible completion blocks release.

Index and bound request/proof/resend/readback work and exact per-principal transitions/per-contact contention; do not globally serialize unrelated contacts, hold transactions across inbox waits, perform provider calls per row or synchronously enumerate historical mail. Declare target deployment attempt/assignment/cardinality, rate/abuse, reconciliation/expiry and latency budgets before activation and test boundaries. For mailing, qualify p95 server reads at most500ms and saves at most1s, excluding network, at50 concurrent mixed requests with100,000 people and100,000 referencing operations per Tenant. These are release targets, not measured performance. Provider configuration, sender capacity and rotation limits must be proved for the actual admitted scope.

Operating signals SHALL retain exact owners and responses:

- Any confirmed wrong principal/Tenant/contact mutation, stale protected admission, unqualified native grant or handoff after withdrawal won is immediately actionable: identity/security with Phase12 or CRM/Phase19 contains the exact path, preserves minimized evidence, repairs through supported owners and requires regression proof before reactivation.
- Any email-change request beyond its declared reconciliation/retention deadline, or required verification/security occurrence beyond admitted dispatch/expiry without resolved outcome, is investigated by identity/data or Phase6 and the producer; reconcile the original operation, never fresh-key resend or guessed cancellation. Any deployed two-proof/session-policy mismatch disables new affected lane until requalification.
- Two consecutive controlled provider-login smoke failures, or one confirmed configuration rejection, are actionable for platform Auth; stop misleading presentation where appropriate and preserve qualified recovery. Apple web-secret30days remaining warns,7days without a verified successor escalates, and expiry disallows use. One confirmed active relay-source failure triggers communications/platform containment and sender repair without central fallback.
- One reproducible lost authorized return/orphaned request or supported keyboard/paste/screen-reader dead end is a donor-experience/identity release defect; repair and rerun the actual journey without sensitive input recordings.
- Mailing fanout unclassified beyond five elapsed minutes alerts CRM/Phase19 while the immediate safety fence remains closed. At least100 requests per15-minute window and p95 read over500ms/save over1s for two consecutive windows, or server errors over1%, alerts API operations to inspect minimized query/lock traces, limit rollout and repeat shaped proof.
- Any retired live raw value beyond24h without exact hold, minimized event beyond its365-day class, or restore making withdrawn contact usable triggers CRM privacy/data restriction and repair without deleting independently governed evidence.
- Five distinct donors per quarter requesting repeated saved-address switching, or three verified valid-address failures in one country within30days, triggers product review of minimized support evidence. Repair format failures immediately; no address library is silently added.

All clocks keep their own owner/start/earlier-stop/hold semantics and cannot renew on read/retry. No universal numeric Auth expiry/throughput or statutory contact-retention promise is invented. Trace every controlling requirement and unnumbered addendum to its contract, user outcome, test and release evidence. The remaining native G01 and per-provider gates are explicit dependencies; whole Phase25 completion still requires the accepted scope or an explicitly changed scope, not a majority of green tests.
