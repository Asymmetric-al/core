# Implementation Plans

Generated by the improve skill on 2026-06-12 against commit `1b2afd5a`
(branch `claude/inngest-durable-workflow-executor`, PR #305). Source: a
high-effort 7-angle code review of the Inngest durable workflow executor
implementation (issues #286ΓÇô#298); 13 findings survived 1-vote verification
(11 CONFIRMED, 1 PLAUSIBLE, 1 confirmed-narrowed). Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                                    | Priority | Effort | Depends on    | Status |
| ---- | ------------------------------------------------------------------------ | -------- | ------ | ------------- | ------ |
| 001  | Fix partial-index upserts (routing reviews + saved routes)               | P1       | S      | ΓÇö             | DONE   |
| 002  | Stripe event recovery (claim backoff, failed-event scan) + pledge guards | P1       | M      | ΓÇö             | DONE   |
| 003  | Inbound pipeline hardening (headers, dedupe, review visibility, retry)   | P1/P2    | M      | 001           | DONE   |
| 004  | Consolidate duplicated helpers + trim wasted I/O                         | P2       | M      | 001, 002, 003 | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 003 requires 001 because review-visibility work (003 Step C) is moot while
  every review-hold write crashes with 42P10 (001 fixes the write path).
- 004 must run last: it refactors the exact code 001ΓÇô003 modify; running it
  first would make their "Current state" excerpts stale.
- 001 and 002 are independent and can run in parallel worktrees.

## Findings considered and rejected (do not re-audit)

- **Envelope context allowlist redesign** (denylist regex in
  `packages/api/src/workflows/events.ts` is approximate): real, but a per-
  workflow context schema registry is a design decision for the PR review
  conversation, not a mechanical fix. Tracked in the architecture report.
- **Stripe webhook dual-path removal** (inline fallback + workflow path must
  stay in lockstep): by design per the OpenSpec rollback boundary ("manual
  recovery paths remain during adoption"). Revisit when the workflow path has
  production mileage; 004 Step 4 reduces the drift surface meanwhile.
- **Inngest-down inline processing fallback**: deliberate trade-off ΓÇö the
  dispatch ledger + (002's) required-keys validation + dead-letter visibility
  are the mitigation; a non-Inngest watchdog belongs in ops tooling, not this
  branch.
- **`set_workflow_dispatch_updated_at` reused by support\_\* tables** (naming/
  ownership): cosmetic until a rollback drops the function; fix by introducing
  a generic `public.set_updated_at` in the NEXT schema migration rather than
  churning applied migrations now.
- **Route-step dead-end when body AND attachments are both `available`**:
  narrowed by review; 003 Step B's bridge-recovery + thrown-step-retry removes
  the main instance; the residual window (routing throws transiently 4├ù with
  everything else complete) is accepted until a generic "re-dispatch inbound
  email" staff action exists.
- **`summaries.ts` per-subject-type loader registry**: deferred as
  architecture work (see architecture report); 004 Step 7 only trims its
  query width.
