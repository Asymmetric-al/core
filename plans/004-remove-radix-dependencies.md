# Plan 004: Remove all Radix dependencies from manifests and lockfile

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md`.
>
> **Drift check (run first)**:
> `rg -n '"radix-ui"|"@radix-ui/' packages apps package.json --glob '**/package.json'`
> Expected: exactly 3 matches, all in the three manifests listed below. If 0,
> the plan is done. If a match appears in any other manifest, a workspace
> gained a Radix dep since planning — report it (see STOP conditions).

## Status

- **Priority**: P1
- **Effort**: S (hours)
- **Risk**: LOW — all three deps are verified unimported; this is manifest + lockfile hygiene
- **Depends on**: plans/001, 002, 003 (so the post-removal typecheck/build gate runs on a green tree; the removals themselves are independent)
- **Category**: migration
- **Planned at**: commit `a661bfb9`, 2026-06-12

## Why this matters

The repo's stated end-state is Base UI only: no `@radix-ui/*` or `radix-ui`
entries in any `package.json` or in `bun.lock`. After the source migration
(plans 001–003), three manifest entries and ~152 lockfile entries remain.
Leaving them invites future agents/developers to import Radix again.

## Current state

Verified:

- `packages/ui/package.json:96` — `"radix-ui": "^1.4.3"` (dependencies). Zero
  `from "radix-ui"` imports remain anywhere in source.
- `apps/admin/package.json:44` — `"@radix-ui/react-visually-hidden": "^1.2.4"` — unused (zero imports in `apps/admin`).
- `apps/donor/package.json:34` — `"@radix-ui/react-tabs": "^1.1.13"` — unused (zero imports in `apps/donor`; donor uses `@asym/ui/components/shadcn/tabs`).
- `bun.lock` — ~152 lines matching `radix` (the rollup package + transitive `@radix-ui/*` packages).
- Root `package.json` declares `"packageManager": "bun@1.4.0"` — all
  dependency operations must use Bun.

## Commands you will need

| Purpose            | Command                                                       | Expected on success      |
| ------------------ | ------------------------------------------------------------- | ------------------------ |
| Remove (ui)        | `cd packages/ui && bun remove radix-ui`                       | exit 0, manifest updated |
| Remove (admin)     | `cd apps/admin && bun remove @radix-ui/react-visually-hidden` | exit 0                   |
| Remove (donor)     | `cd apps/donor && bun remove @radix-ui/react-tabs`            | exit 0                   |
| Reinstall/lock     | `bun install` (repo root)                                     | exit 0, lockfile saved   |
| Lockfile gate      | `rg -c "radix" bun.lock`                                      | no matches (exit 1)      |
| Pkg tree gate      | `bun pm ls \| rg -i "radix"`                                  | no matches               |
| Typecheck          | `bun run typecheck`                                           | exit 0                   |
| Workspace contract | `bun run verify:workspace-contract`                           | exit 0                   |

## Scope

**In scope**:

- `packages/ui/package.json`, `apps/admin/package.json`, `apps/donor/package.json`
- `bun.lock` (regenerated by Bun — never hand-edited)

**Out of scope** (do NOT touch):

- Any source file, any other manifest, `vendor/**` (vendored Payload upstream
  keeps its own pnpm lockfile — explicitly out of the active frontend),
  `node_modules/**`.

## Git workflow

- Branch `claude/naughty-knuth-09f32b`; no commits/pushes unless instructed.

## Steps

### Step 1: Remove the three dependencies

Run the three `bun remove` commands from the table (each from its package
directory), then `bun install` at the repo root.

**Verify**: `rg -n '"radix-ui"|"@radix-ui/' packages/*/package.json apps/*/package.json package.json` → no matches

### Step 2: Lockfile and tree gates

**Verify**: `rg -c "radix" bun.lock` → no matches; `bun pm ls | rg -i "radix"` → no matches

### Step 3: Full repo gates still green

**Verify**: `bun run typecheck` → exit 0; `bun run verify:workspace-contract` → exit 0

## Test plan

No new tests. `bun run test:unit` once at the end → all pass (guards against
accidental resolution changes from the reinstall).

## Done criteria

- [ ] `rg -n '"radix-ui"|"@radix-ui/' packages apps package.json --glob '**/package.json'` → no matches
- [ ] `rg -c "radix" bun.lock` → no matches
- [ ] `bun pm ls | rg -i "radix"` → no matches
- [ ] `bun run typecheck` exits 0
- [ ] `bun run test:unit` exits 0
- [ ] Only the three manifests + `bun.lock` changed (`git status`)
- [ ] `plans/README.md` status row updated

## STOP conditions

- `bun install` fails or rewrites unrelated dependency versions beyond
  removing radix entries — report the diff instead of forcing it.
- Any `rg` gate still finds a radix entry in a manifest other than the three
  listed (a workspace gained a Radix dep since planning) — report it.
- Typecheck breaks after removal (would mean a hidden Radix import survived
  plans 001–003) — report the importing file.

## Maintenance notes

- `vendor/payload-upstream/pnpm-lock.yaml` and
  `vendor/payload-upstream/examples/**` legitimately contain Radix — they are
  vendored third-party code outside the active frontend; the final audit
  (plan 006) records them as the only allowed matches.
- If CI uses a frozen lockfile, the regenerated `bun.lock` must be committed
  together with the manifest changes.
