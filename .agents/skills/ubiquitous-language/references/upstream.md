# Ubiquitous language (upstream)

This skill is vendored from **mattpocock/skills** at  
`skills/deprecated/ubiquitous-language/SKILL.md`.

It is **deprecated** in upstream’s Skills CLI surface (`npx skills add --skill` lists only active skills), but remains useful for domain glossary / “domain model” style workflows.

Refresh: copy `SKILL.md` from the upstream path above (same commit as other mattpocock pins), update `skills-lock.json` `computedHash`, run `bun run skills:sync` and `bun run skills:verify`.
