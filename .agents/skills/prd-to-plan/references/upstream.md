# prd-to-plan

Not published as a separate skill in **mattpocock/skills** (as of the pin used in this repo). This directory is a **repo-local router** so agents that look for `prd-to-plan` get a deterministic path. Use **to-prd**, **to-issues**, and OpenSpec per root **`AGENTS.md`**.
