# QA (upstream)

Vendored from **mattpocock/skills** at `skills/deprecated/qa/SKILL.md`.

The Skills CLI does not list deprecated skills; copy from upstream when refreshing.

Refresh: update files from that path, `skills-lock.json` `computedHash`, `bun run skills:sync`, `bun run skills:verify`.
