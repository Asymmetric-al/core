/**
 * Re-export of the canonical Support Hub query keys. Owned by
 * `@asym/database/query-keys` so the database collections, server reads, and
 * client hooks all reference the same registry.
 */
export { supportHubQueryKeys } from "@asym/database/query-keys";
