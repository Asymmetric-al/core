import type { Metadata } from "next";

export { default } from "./worker-feed-page-client";

export const metadata: Metadata = {
  title: "Feed",
  description: "Follow updates and moderate worker activity in one place.",
};
