# Design

## Accepted scope

Read `docs/ai/document-authority.md` and the source manifest for AL-1861.
Import each ratified source delta at its pinned PR head, reconcile shared owner
indexes cumulatively, and preserve untouched package bytes and original history.

## Publication prerequisite

The user explicitly approved including PR #1428 at
`4e2c014afdbea1a2679f71ff3e9f385f6bf755a2` as a prerequisite. Its 11 original workflow
commits and subsequent develop-integration merge remain intact. Its trusted identity registry and normal hook/CI proof
replace the inherited singleton attribution policy, which rejected Conrad and
the GitHub-authored develop baseline. The five overlapping files are reconciled
cumulatively; the workflow source manifest records exact source and integrated
hashes. No credential, repository setting or required approval is bypassed.
The first large documentation commit required a narrow metadata-reader fix:
project the fields consumed by attribution inside `gh` before buffering stdout.
The default subprocess limit and all identity/signature/parent checks remain
intact; file patches, commit messages and unused profile fields are excluded.

## Authority

Explicit current founder instructions and scoped accepted owner amendments
resolve stale normative prose. Generated views follow their canonical authoring
source. A current-runtime claim requires source/test/environment evidence.

## Verification and risk

Use strict OpenSpec plus read-only delta applicability, source manifest coverage,
P25 generation parity, formatting and links. Add negative tests only for new
verifier behavior. Do not run archived reference models in their capture folders.
Keep all imported feature implementation/qualification tasks unchecked. Existing
accepted ADR names remain stable; a registry and full links disambiguate them.

## Recovery

Revert only this reconciliation. Historical source hashes identify the original
packet; do not rewrite them to match later edited docs. No product state or
external provider was changed. New product tradeoffs require a separate ruling.
