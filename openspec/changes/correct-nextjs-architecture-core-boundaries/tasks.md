## 1. Evidence And Corrections

- [x] 1.1 Verify actual API exports, server/client requirements in installed
      Next.js docs, API-owned cache tags and existing callable routes.
- [x] 1.2 Reproduce five surviving local-annotation deletion mutations; bound
      the assertions and prove the same five mutations are rejected.
- [x] 1.3 Add a package-export contract check, observe the invalid API-root
      example fail, and replace invented examples with actual contracts.
- [x] 1.4 Correct Core read, client-hook, mutation and cache remaps in the
      workflow and linked references without changing the upstream pin/hash.

- [x] 1.5 Re-evaluate resolved review histories and qualify every reference API against installed docs or primary behavior evidence.
- [x] 1.6 Correct reference recipes and refresh instructions; prove pending attributes and canonical optimistic completion through executable React examples, and require payload-bearing action results through the TypeScript checker.

## 2. Verification

- [x] 2.1 Regenerate all runtime mirrors through skills:sync and verify equality.
- [x] 2.2 Pass focused skill/routing tests, the five mutation controls after
      final edits, formatting, strict OpenSpec validation and diff hygiene.
- [ ] 2.3 Complete repository PR gates and publish the verified correction;
      leave the OpenSpec change active until implementation merges.
