## Why

The imported Next.js architecture skill still gives conflicting client/server,
API-export and cache-ownership instructions after its Core placement remap.
Unbounded regression assertions also accept removal of the exact local remaps.

## What Changes

- State server reads, client table hooks, supported mutation transports and
  API-owned server cache tags at the relevant Core workflow steps.
- Replace invented Core API examples with actual published contracts while
  retaining labeled upstream illustrations and their reviewed provenance.
- Correct resolved-review recipe defects using installed Next.js contracts: explicit pending attributes, canonical optimistic commits, metadata/cache prerequisites, variable-height grouping, and truthful portal snapshots.
- Use a pinned CLI and verified immutable checkout before refresh copying, retain raw provenance and local adaptations, and preserve every required runtime mirror.
- Bound each remap assertion to its own instruction and verify example imports
  against the API package export map; regenerate all runtime mirrors.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `agent-instruction-system`: architecture skill guidance must preserve Core's
  callable boundaries, package ownership and independently verified remaps.

## Impact

Canonical `nextjs-app-architecture` skill files, generated Codex/Cursor/Claude
mirrors and focused documentation contract tests. Application code, API exports,
dependencies, upstream pins, root AGENTS and product behavior are unchanged.
This does not refresh the vendor skill or prescribe a new app folder scheme.

## Rollback

Revert the canonical guidance and guard changes together, regenerate mirrors,
and rerun focused tests and mirror verification. No runtime/data rollback exists.
