# Tasks

## 1. Spec deltas

- [x] 1.1 Write MODIFIED/ADDED deltas for `platform-product-intent`
      (customer model, outcome-anchored success, ladder deference, AI
      direction).
- [x] 1.2 Write MODIFIED delta for `platform-principles` (canonical priority
      ladder).
- [x] 1.3 Write MODIFIED/ADDED deltas for `platform-surfaces` (Support Hub
      domain, governed outbound communications channel).

## 2. Config and context

- [x] 2.1 Rewrite `openspec/config.yaml` to the documented v1.x schema with
      injected context and per-artifact rules.
- [x] 2.2 Update `openspec/project.md`: two-layer spec model, current spec
      table, archive discipline.
- [x] 2.3 Update `CONTEXT.md` glossary: sharpened Tenant, new Ministry Update.

## 3. Validation

- [x] 3.1 `bunx @fission-ai/openspec@latest validate --all --strict` passes.
- [x] 3.2 Prettier format check passes for touched markdown files.
