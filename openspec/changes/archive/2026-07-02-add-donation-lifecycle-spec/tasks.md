# Tasks

## 1. Author spec

- [x] 1.1 Map shipped donor-side behavior with file evidence (checkout, saga,
      webhooks, recurring, receipts, portal).
- [x] 1.2 Write the `donation-lifecycle` delta from verified behavior only; no
      aspirational requirements.

## 2. Validation

- [x] 2.1 `bunx @fission-ai/openspec@latest validate --all --strict` passes.
