# Tasks

## 1. Contract and evidence foundation

- [ ] 1.1 Freeze the Document Purpose Contract, Approved Data View, Facts
      Package, publication graph, generation, issuance-link, artifact, batch,
      access, records, and appointment schemas with closed versioned states and
      reason codes.
- [ ] 1.2 Publish the U.S. and Canadian purpose/case manifests and obtain the
      required current qualified legal, finance, records, security, privacy, and
      accessibility review evidence. Keep unproved purposes dark.
- [ ] 1.3 Pre-register and hash the renderer contest charter, open and held-back
      synthetic corpora, validators, hard gates, scorecard, reviewers, budgets,
      and no-winner rule.
- [ ] 1.4 Run the exact Prince-family and Typst 0.15.1 finalists plus Chromium
      control, retain all evidence, and select at most one qualified production
      deployment. Do not implement official rendering if no candidate passes.
- [ ] 1.5 Amend Phase 17 protected-action behavior and ADR-0025 to the
      selector-plus-fragment-verifier protocol required by ADR-0037.

## 2. Canonical persistence and tenant safety

- [ ] 2.1 Create the final bounded `pdf_*` document-production schema directly:
      stable template roots; mutable CAS drafts; immutable candidates and
      publications; heads and one appointment; facts references; idempotent
      requests; fenced attempts; logical documents; exact artifacts; source-owned
      issuance links; batches/items; access grants/sessions; schedules/holds/
      disposal; and PII-minimal audit evidence. Official financial Facts
      Packages and artifacts pin the exact source-facts version and Legal
      Entity without importing Phase 20 accounting projection authority.
- [ ] 2.2 Enforce non-null tenant/environment and, where applicable, exact-issuer
      scope; composite same-scope foreign keys; restrictive deletes; immutable
      pins; named uniqueness; one-current-head/artifact; nonoverlapping Canadian
      coverage/signer intervals; serial non-reuse; and append-only transitions.
- [ ] 2.3 Add least-privilege server capabilities, RLS/`FORCE RLS`, private
      storage, object authorization, opaque never-reused object identities, and
      permanent hostile cross-tenant/environment/issuer/Party tests.
- [ ] 2.4 Implement the single product-level Generated Document service. Keep
      all staff, donor, missionary, API, batch, scheduled, correction, and
      delivery routes as thin adapters over it.
- [ ] 2.5 Prove database idempotency, current-head CAS, worker fencing,
      provider-deadline races, attempt cleanup, artifact promotion, correction
      lineage, serial allocation, hold/disposal races, and appointment activation
      with real Postgres/Supabase behavior.

## 3. Authoring and publication

- [ ] 3.1 Build the flow-first structured visual editor, equivalent outline/
      non-drag controls, bounded block catalog, page constraints, protected truth
      rails, reusable copied sections, soft expiring presence hints, server-CAS
      conflict-safe autosave, and accessible responsive states. Presence must
      never grant authority or hard-lock another authorized author.
- [ ] 3.2 Integrate the Phase 11 field catalog into tenant-extensible Approved
      Data Views with semantic IDs, bounded source-owned roles/collections,
      source-owned cases, safe formatting, and structurally absent forbidden
      fields.
- [ ] 3.3 Build synthetic layout preview, exact proof generation, grouped checks,
      semantic/visual comparison, immutable commit, standard/protected review
      floor, atomic publish, restore-as-new-draft, and publication impact.
- [ ] 3.4 Implement D15 resolution: configured inheritance first, then the
      closed proof-gated recovery list of at most one affirmatively compatible
      same-scope/exact-locale prior followed by one exact-locale purpose-permitted
      ancestor, with first-compatible-wins and prior winning when both pass;
      forbid tenant priority settings/DSL and sibling, foreign-locale, and system
      recovery; atomically resolve/freeze; retry exact pins; stop on safety-live
      changes; and permit a proof-gated successor only for definitively unissued
      work.
- [ ] 3.5 Implement publish-now-first plus one immutable future appointment per
      head, with civil/IANA/offset/fixed-UTC/tzdb-generation evidence, completed-
      approval survival across ordinary offboarding, explicit security/governance
      invalidation, independent protected review, current safety reproof, due-
      request barrier, bounded recovery, and explicit keep-scheduled versus
      publish-now-and-cancel conflict handling.
- [ ] 3.6 Implement the versioned Asym-native semantic-template export/import
      package with exact package/version/schema/dependency digests, source-export
      and destination-import authorization, integrity and compatibility/loss
      reporting, destination-owned quarantined drafts, normal reproof/review/
      publication, prohibited-state/data rejection, no auto-publish, and no
      legacy or foreign-format converter.

## 4. Artifact production and access

- [ ] 4.1 Implement the selected renderer behind the narrow contract with frozen
      no-network assets/fonts, bounded resources/deadlines, exact profile inputs,
      deterministic diagnostics, and no cross-engine fallback.
- [ ] 4.2 Finalize, validate, hash, store, read back, and atomically promote one
      private canonical `accessible-v1` or `accessible-archive-v1` PDF. Preserve
      exact bytes and evidence; join each hash to the shared externally anchored
      append-only checkpoint with `anchored`/`unanchored_pending` reconciliation
      that never blocks authorized access or changes currentness; never generate
      delivery renditions or a Phase 18 signing service.
- [ ] 4.3 Implement one logical-document/current-head projection, exact copy,
      correction/replacement, evidence-only history, and authorized staff/donor/
      missionary read paths.
- [ ] 4.4 Implement authenticated portal access and the ADR-0037 guest handoff:
      selector plus fragment verifier, inert `GET`/`HEAD`, deliberate same-origin
      POST, recipient-epoch authorization, bounded grant rotation, no-store exact
      streaming, and non-enumerating recovery.
- [ ] 4.5 Hand only exact artifact references to Phase 17. Prove that provider
      delay, bounce, complaint, duplicate webhook, resend, and message fallback
      never alter artifact or issuance truth.

## 5. Jurisdiction packs

- [ ] 5.1 Implement the complete everyday U.S. acknowledgment pack, source-
      selected cases, separate QCD/DAF semantics, specialty obligations, opaque
      `ACK-XXXXX-XXXXX` identity, immutable `vN` corrections, and exact legal/
      semantic fixtures.
- [ ] 5.2 Implement tenant-silent Canadian setup and activation with one verified
      registered-charity issuer, four ordinary cases, cumulative cash coverage,
      ordinary approved Quebec overlay, specialist obligations, and zero runtime
      impact for nonparticipants.
- [ ] 5.3 Implement exact-issuer `R-` lifetime allocation through the
      Phase 18 D11 allocator only after source authorization and admitted request
      freeze, with safe initial-position evidence, permanent nonreuse/disposition,
      exact issuance/artifact linkage, exact copy, source-authorized formal new-
      serial replacement, finance register, and concurrency/recovery proof.
      Templates, renderers, callers, staff, and source commands never allocate;
      source domains retain issuance validity and correction-effect authority.
- [ ] 5.4 Implement one current Canadian signer plus at most one ready successor,
      protected sanitized mark, effective intervals, exact preview, authority
      fencing, and exact-byte/WORM/audit-checkpoint evidence without tenant PKI.
- [ ] 5.5 Keep Canadian production and protected delivery dark until the exact
      current qualified review and every production-shaped release gate passes.
- [ ] 5.6 Gate each exact Canadian issuer on finite current records readiness:
      required production primary and backup/recovery location in Canada,
      production readability, production/recovery decryption, representative
      exact-byte-and-metadata restore, and versioned tenant-responsibility
      acknowledgement. Prove incomplete/drifted evidence blocks only new affected
      Canadian issuance and nonparticipants remain structurally untouched.

## 6. Batches, records, and operations

- [ ] 6.1 Implement item-authoritative batches over the same request seam with
      frozen item sets/pins, preflight, mixed outcomes, retry-failed-only,
      ambiguity exclusion, tenant fairness, poison isolation, resumability, and
      grouped repair. Leave Phase 19 population/run scheduling external.
- [ ] 6.2 Implement the seven closed Records Schedule obligations, preservation
      floors/privacy ceilings, reviewed transitions, monotonic scoped holds,
      restricted custody, owner coordination, and Canadian nonparticipant
      silence.
- [ ] 6.3 Implement staged exact-version disposal, primary/derived/provider
      verification, backup-expiry truth, forward-only restore suppression,
      PII-minimal proof, and restore/export/readability drills.
- [ ] 6.4 Add PII-safe traces, metrics, reconciliation, SLOs, owner/reason/next-
      action exceptions, capacity and integrity alerts, and concise operator
      runbooks for every terminal failure class.
- [ ] 6.5 Implement one permissioned Records disclosure with controlling purpose/
      authority/dates/version/explanation; contract-enumerated bounded extensions
      using expected-version and audited-basis fencing; scoped hold/release; and
      custody/disposition evidence export. Reject floor weakening, unsupported/
      forever/per-document retention, direct deletion, and unreviewed privacy-
      ceiling breaches.
- [ ] 6.6 Guard activated-Canadian tenant closure until either a verified
      destination-custody transfer or explicit restricted records-only custody
      agreement preserves the exact issuer, immutable history, holds,
      restrictions, and readable records. Keep the entire guard absent for
      nonparticipants.
- [ ] 6.7 Implement law/policy/provider schedule change as one bounded impact set
      and qualified-reviewed effective-dated successor, with expected-version and
      idempotency fencing, preserved prior schedule/version/trigger evidence, and
      shortening routed through ordinary grace, final reproof, holds, and verified
      disposal rather than immediate mass deletion.
- [ ] 6.8 Persist disposal proof through the closed allow-list only: record class,
      tenant/issuer, schedule version, reason, operation/time, copy-class
      outcomes, and narrow D11 nonreuse disposition. Reject names, addresses,
      amounts, filenames, paths, content, raw Party IDs/hashes, and bind the proof
      to its own bounded schedule.

## 7. Clean pre-production cutover

- [ ] 7.1 Run the server-authoritative environment/data/dependency assertion.
      Stop and re-groom if any production tenant, irreplaceable artifact, or
      external dependency exists.
- [ ] 7.2 In the proved pre-production environment, remove the live receipt/
      statement text routes, hard-coded receipt sender, receipt snapshots as
      artifact authority, placeholder receipt records, Unlayer/native runtime
      switch, mutable production roots, browser official facts, broad direct
      writes, provider-URL truth, and every alternate writer/reader.
- [ ] 7.3 Reset/rebuild the disposable environment into only the final schema and
      private custody posture; regenerate types and verify no legacy row,
      adapter, route, flag, dependency, or product surface remains.
- [ ] 7.4 Run the full end-to-end U.S. tracer and the gated Canadian corpus from
      source facts through publication, generation, exact artifact, authorized
      access, Phase 17 delivery, resend, correction/replacement, retention,
      restore, and disposal.

## 8. Release verification and documentation

- [ ] 8.1 Pass unit/property, real-database/RLS, contract, concurrency,
      fault-injection, security/IDOR, accessibility, visual, PDF semantic/PDF-UA/
      PDF-A, locale/font, batch/load/fairness, storage/restore, records, and
      cutover test suites through the confirmed public seam.
- [ ] 8.2 Prove representative staff can create, preview, publish, schedule,
      generate, batch, find, open, correct, deliver, and repair without developer
      help or implementation vocabulary, and donors see one current authorized
      document action.
- [ ] 8.3 Keep the Phase 18 PRD, ADR-0033 through ADR-0039, OpenSpec, Phase
      7/13/17/19 contracts, roadmap, phase map, parity matrix, guides, and published
      tickets congruent. Preserve the published epic #907 and children
      #908–#961: #908–#910 are the approved `status:todo` +
      `ready-for-agent` frontier, #911–#961 remain `status:blocked`, and this
      task authorizes no further dispatch.
- [ ] 8.4 Archive this change only after implementation has shipped, every gate
      is proven, and the delta is merged into the durable capability specs.
- [ ] 8.5 Add public-seam acceptance tests for the records-admin projection,
      permitted extension, stale/forbidden mutation, hold/release, and evidence-
      export behaviors; both successful Canadian closure paths; incomplete-
      custody closure blocking; and complete Canadian-nonparticipant absence.
- [ ] 8.6 Add deterministic resolver tests proving configured inheritance is
      normal primary resolution, same-scope/exact-locale prior precedes the
      exact-locale permitted ancestor, both-compatible selects the prior, and no
      sibling, foreign-locale, system, recursive, scored, or tenant-configured
      recovery path exists.
- [ ] 8.7 Add schedule-transition and minimal-proof tests covering a shortening,
      preserved prior evidence, stale/duplicate transition delivery, no immediate
      mass delete, the complete disposal-proof allow-list, every forbidden field,
      hostile provider payloads, and the proof's bounded schedule.
