## Why

Phase 44 needs an explicit accountable successor to its predecessor reservations, with a complete user journey and verifiable source/provider boundaries. AL-1892 adopts the supplied roadmap planning without claiming implementation or runtime activation.

## What Changes

- Adopt the complete `enterprise-identity` planning package, all 12 work-package IDs and all 34 original acceptance cases.
- Bind exact existing identity, permission, communication, connection and domain owners; preserve predecessor generation scope and negative tests.
- Require the declared capability checkpoints, selected-profile decisions, real evidence, migration, recovery and accessible user journeys before completion.
- Keep the change active and every implementation task unchecked until accepted implementation evidence exists. Local IDs are not published GitHub child issues.

## Capabilities

### New Capabilities

- `enterprise-identity`: the complete Phase 44 successor product through its source-owned, profile-qualified boundaries.

### Modified Capabilities

None. Existing source capabilities and their generation-specific restrictions remain controlling. This new successor does not weaken or globally replace an earlier inactive generation.

## Impact

Canonical planning: `docs/prds/enterprise-identity/specification.md`, `qualification.md`, and `delivery.json`. Cross-product allocation: `docs/prds/program-roadmap/integration-guide.md` and the canonical program roadmap. Source reference labels are scoped to their preserved package, not allocated ADR numbers.

Future implementation affects the existing admin/donor/missionary surfaces only where source-authorized, shared `packages/api` business commands, `packages/auth` sessions, approved `packages/database` projections, shared `packages/ui`, tenant-safe stores, server-only credentials and shared execution/claims. Physical schemas, exact SDK/provider versions and unresolved contract details require the listed qualification records first.

This planning change performs no provider operation, migration or product activation. Runtime rollout must fence incompatible writers, prove source readiness, admit only selected qualified profiles and preserve old-generation safety. Containment stops favorable/new effects while retaining authenticated or already accepted negative work, source history and exact recovery; it must not restore a broad legacy writer or replay uncertain external work.
