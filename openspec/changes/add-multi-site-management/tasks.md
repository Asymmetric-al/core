# Tasks

## 1. Authority and prerequisite reconciliation

- [ ] 1.1 Reconcile the merged Phase 2/5/12/13/16/17/20 contracts and the
      accepted equivalent of Phase 23 Public Site Generation, Working Revision,
      Page placement, finite transfer, lease, and owner-adapter seams. Record
      every current-code conflict; do not treat open PR #1340 as shipped.
- [ ] 1.2 Freeze the Phase 24 owner map, command/result vocabulary, stable
      identities, state machines, exact capabilities, the distinct Site Locale
      Publication Contract and Domain Critical Owner-Family Registry, route
      classes, Money metadata profile, provider adapter outcomes, and migration
      census contract.
- [ ] 1.3 Maintain and validate the committed Phase 24 traceability matrix from
      stories 1–120 and D1–D18/D57–D84 through OpenSpec scenarios, ADRs, design,
      tasks, executable tests, and release evidence. Add a CI validator that
      rejects duplicate IDs, invalid targets, or uncovered normative entries.

## 2. Canonical persistence and authorization

- [ ] 2.1 Add the final operational Site, lifecycle, Default Site, Site Locale,
      Default Site Locale Version/Plan, Site Brand Version, currency policy,
      Suggested Amount Set, Domain claim/binding/role/barrier/operation,
      public-route allocation, translation/copy, Page-continuity, and handoff
      records without a generic settings or workflow table.
- [ ] 2.2 Enforce full structural Tenant/Site/locale/owner scope, same-scope
      composite foreign keys, required non-null identity, one-current/global-
      host/cardinality/check constraints, immutable heads/history, checked Money,
      and restrictive deletes.
- [ ] 2.3 Revoke browser authoritative DML; add operation-correct RLS `USING`
      and `WITH CHECK`, invoker-safe views, hardened typed command functions,
      trusted actor/scope derivation, and equivalent service/worker/Payload/
      import/support enforcement.
- [ ] 2.4 Implement the shared expected-head, semantic-idempotency, deterministic-
      lock, durable-receipt, audit, outbox, claim/fencing, and unknown-result
      readback primitives through existing business/data-access boundaries.
- [ ] 2.5 Prove all constraints, grants, RLS, privileged paths, concurrent
      winners, failpoints, retries, receipt reconciliation, and cross-Tenant/
      Site/locale poison cases against real disposable Postgres.

## 3. Site setup, brand, activation, and lifecycle

- [ ] 3.1 Build private Add Site and one-item exact-revision Copy a Page or
      Article, with descriptive purpose, Default Site staff preselection, no
      inherited authority, and zero public/provider/money effect.
- [ ] 3.2 Build complete immutable Site Brand Version authoring, production-
      renderer preview, qualification, publish/restore-as-successor, safe asset
      failure, mandatory complete-version Go-live/public-generation gating, and
      exact public-generation pinning in the existing Web Studio shell.
- [ ] 3.3 Build D6 Go live with complete private preview, exact readiness,
      expected-head atomic activation, immutable generation, receipt, audit,
      outbox, and honest Publishing/readback states.
- [ ] 3.4 Build independent Take website offline and Pause new gifts from this
      Site policies with monotonic admission fence, exact consequence UX,
      pre-admitted gift behavior, owner-safe recovery, and convergence proof.
- [ ] 3.5 Build terminal Site retirement/Discard setup with complete current
      impact, Default successor, terminal nonreuse, retained owner history, and
      zero implicit redirect/copy/money effect.

## 4. Tenant donor account and message projection

- [ ] 4.1 Build one verified Tenant Donor Portal Host per Tenant/environment,
      host-only secure session/callback behavior, synthetic auth/recovery proof,
      complete DNS/TLS/platform/callback/legal/support qualification,
      successor-first replacement, neutral no-fallback host-loss behavior, and
      separately governed support/recurring-cancellation access.
- [ ] 4.2 Build one complete immutable Tenant Donor Account Brand covering every
      account/auth/recovery/error state; allow only bounded presentation inputs
      and safe Site attribution/return context.
- [ ] 4.3 Add the compact read-only Phase 17 Messages projection to applicable
      Site views with exact Ready/fallback/attention/checking/unavailable states,
      permission-safe grouped exceptions, and one owner action. Add no Site-owned
      message configuration or readiness persistence.

## 5. Exact-locale public management

- [ ] 5.1 Replace interim locale arrays with stable relational Site Locale
      identity and immutable exact BCP 47/profile facts through one-way reader-
      first migration; fence every legacy writer before activation.
- [ ] 5.2 Implement the fixed Site Locale Public Base, typed route registry,
      permanent `/lang/` reservation, route allocation/history, exact-locale
      public generation, no-fallback reads, metadata/sitemap closure, and fully
      scoped cache identity.
- [ ] 5.3 Implement the deterministic Site Root Entry method/header/query/
      fragment contract and sole Default Site Locale head with direct final
      locale links, one-hop `307`, no language negotiation, and bounded
      generation-aware caching.
- [ ] 5.4 Build the undated private Default Site Locale Plan with source-owned
      blockers, permission-filtered readiness, explicit supersession/
      cancellation, D16 activation, terminal cleanup, and recursive rejection
      of Plan dates/priorities/schedules/reminders.
- [ ] 5.5 Implement the small versioned five-family critical-path inventory,
      exact source adapters, private production preview, explicit first/
      additional locale publication, explicit `sites.publish_locales` whole-
      locale withdrawal/default-and-dependency blockers, adverse-fence-first
      transition, separately reauthorized restoration, complete generation
      acknowledgement, and ordinary missing-content behavior.
- [ ] 5.6 Prove exact-locale, default/root, BCP 47, route classification,
      canonical/alternate/sitemap, public status, cache, accessibility, RTL/CJK,
      no-JS, and Phase 5 p99 behavior in the vertical tracer and load suite.

## 6. Translation and copy

- [ ] 6.1 Add explicit Translated/Independent/Legacy provenance, compatible
      Translation Basis, authoritative source-input identity, ordinary Out of
      date projection, reviewed-current successor, and source-owned safety
      disposition with smallest adverse closure.
- [ ] 6.2 Add authoring-only Suggested translation sources with expected-
      revision save, accessible reorder, point-of-use authorization, conflict
      recovery, and zero public/cache/provenance effect.
- [ ] 6.3 Implement exact Latest saved draft and Current published version
      logical heads from D12 Working Revision and D1 current generation only;
      exclude in-flight/history/schedule/provider-latest candidates; implement
      qualified-public-only deduplication, immutable Copy Source Checkpoints,
      public-source first-publication gate, and atomic one-target Copy command.
- [ ] 6.4 Implement exact-revision Copy Qualification with complete input
      treatment, bounded immutable evidence, lossless command-time recheck,
      source-owned non-gating findings, and independent target validation.
- [ ] 6.5 Build the qualified Source version RadioGroup followed immediately by
      Unavailable source versions, non-enumerating dispositions/actions, stable
      paging/snapshot, and direct Start blank draft zero-choice path.
- [ ] 6.6 Prove source/public/private/dedup heads, stale/lost-ack cases, retained
      checkpoint/Basis, lossless manifest boundaries, hidden-source privacy,
      maximum-catalog p95 300 ms, and accessible Sheet/focus behavior.

## 7. Domain authority and Vercel integration

- [ ] 7.1 Implement canonical host normalization, platform-wide occupancy,
      binding intervals, exactly one Primary per public Site, optional Redirect
      roles, route-owner precedence, one-hop clean navigation, and provider-
      independent public generation.
- [ ] 7.2 Build explicit former-primary Redirect/Stop review, deterministic
      route outcomes, compatible origin generation, current-head transaction,
      and no provider whole-domain redirect authority.
- [ ] 7.3 Build owner-cleared self-service disconnection with complete finite
      manifest, Disconnecting barrier, cohort acknowledgement, sealed exact
      Vercel removal, authenticated absence, final claim release, and durable
      Needs attention reconciliation.
- [ ] 7.4 Build fresh seven-day 256-bit exact-host TXT proof, one-use challenge
      consumption, one-winner global claim, new private binding, non-enumerating
      conflicts, provider-after-claim preparation, and separate TLS/DNS/public
      readiness.
- [ ] 7.5 Build prepared same-Tenant Site-to-Site move with both-Site authority,
      unselected destination role, source Primary replacement, Moving adverse
      generation, atomic source/destination/global-head swap, persistent progress,
      and no provider/DNS/TLS/content/money migration.
- [ ] 7.6 Implement the distinct small Domain Critical Owner-Family Registry
      and deterministic complete Domain Move Route Review using its exact typed
      results, pure comparison outcomes, permanent source-only not-found effects,
      exact collision blockers, and exception-first Existing web addresses UX.
- [ ] 7.7 Implement and qualify the narrow Vercel owner adapter with exact
      idempotent resources, normalized outcomes, coalescing, current limit/
      header-aware backoff, no network call under lock or public request, and
      authenticated ambiguity reconciliation.

## 8. Giving addresses and managed placement convergence

- [ ] 8.1 Implement permanent Issued Giving Address allocation for the exact
      Site Locale Public Base plus `/give/{staff-chosen-slug}`, canonical
      equivalents, nonreuse, private draft availability, atomic issue, and
      privacy-safe collision alternatives.
- [ ] 8.2 Implement per-locale Preferred for sharing head, direct continued
      service of older eligible addresses, capability-gated preference change,
      separate terminal Stop, admitted-operation result preservation, and zero
      redirect/reassignment.
- [ ] 8.3 Implement Prepare authorized updates through registered source-owner
      ports, stable placement references, private dependency-only candidates,
      source-owned review/release/send, itemized resumable receipts, and truthful
      unsupported/frozen/external classifications.
- [ ] 8.4 Prove host/locale/route/meaning invariants, concurrent issue/preference/
      Stop, rebinding/retirement, no Giving redirect, no hidden placement leak,
      no immutable-output rewrite, and exact donor/public outcomes.

## 9. Currency, Money, and donor transitions

- [ ] 9.1 Replace every authoritative USD/two-decimal and floating conversion
      seam with checked integer minor-unit Money plus versioned ISO/exponent
      metadata; audit every formatter, parser, receipt, refund, recurring,
      ledger, export, idempotency, and provider boundary.
- [ ] 9.2 Implement immutable Site currency-policy versions and Payments-owned
      route/mode offering-envelope qualification plus later amount/method and
      final exact checkout reproof, with side-effect-free Stripe evidence,
      expiry/drift, current-fingerprint save, and no public-read provider call.
- [ ] 9.3 Build the compact Site Currencies card with default/enabled intent,
      per-mode results, one cause-owned action, stale/unknown preservation, and
      no provider/accounting wizard.
- [ ] 9.4 Implement donor pristine-currency resolution and explicit selector,
      ISO code/name visibility, qualified default/fallback-to-explicit-choice,
      single-currency cart, and final exact checkout reproof.
- [ ] 9.5 Implement donor currency transition with preserve/clear classification,
      consequence-specific accessible confirmation, source-cart CAS, stale
      provider-attempt fence, target unanswered amount, and no FX/digit carry.
- [ ] 9.6 Implement immutable zero-to-six Suggested Amount Sets and one shared
      staff editor/donor choice component across every giving surface, including
      custom-only, deliberate disable-retirement, explicit re-enable
      reaffirmation, and transient-qualification preservation without bypass.
- [ ] 9.7 Implement exact Phase 16 schedule transition for one editable line,
      preserving purpose/siblings, clearing the smallest dependent closure,
      rebuilding target schedule/group state, and forbidding accepted/provider-
      unresolved mutation.
- [ ] 9.8 Prove JPY/BHD/ISK/HUF/TWD boundaries, every supported directed cadence
      transition and no-op, pristine/material confirmations, guest/auth two-tab
      retries, provider lifecycle, no platform-account fallback, no historical
      rewrite, and full downstream money conservation.

## 10. Ordinary Page continuity and material-purpose handoff

- [ ] 10.1 Implement exact D78 fixed-pair structural eligibility and unselected
      human successor qualification as immutable directional, path-specific,
      nontransitive prepared Page-owner evidence with zero public/route/Page/
      provider/donor effect until exact rechecked owner consumption.
- [ ] 10.2 Implement sparse Page Purpose Continuity Versions, meaning-bearing
      dependency digests, conditional existing Publish review choice, multiple-
      predecessor status, and zero runtime purpose lookup.
- [ ] 10.3 Implement the contextual material-purpose handoff review using the
      exact acknowledged candidate and finite transfer compiler, with one fresh
      private Page and no inherited public/owner/provider authority.
- [ ] 10.4 Implement the single D80–D84 transaction: protected source Editorial/
      Placement checkpoints, changed-axis clean successors, lease fencing,
      exact never-public path adoption when positively proved, complete private
      descendant re-derivation, and provenance-safe sibling position/default.
- [ ] 10.5 Prove every transfer member classification, source-axis combination,
      path claimant/history condition, descendant member outcome, placement
      provenance/boundary, concurrency order, failpoint, lost response, stale
      lease/tab, retention/hold, and zero public/Navigation/provider/money effect.

## 11. Public runtime, caches, and performance

- [ ] 11.1 Extend the published-content reader to require exact Site/host/locale/
      generation context and consume only immutable admitted generations;
      remove or fence null-Site, mutable-latest, host-blind, and public-provider
      reads.
- [ ] 11.2 Implement full-scope cache keys, no-store adverse/private/preview
      responses, exact scoped invalidation, immediate adverse bypass, and compact
      fail-closed edge admission without using tags as isolation.
- [ ] 11.3 Prove Site-suspension p99 acknowledgement ≤5 seconds and zero required
      favorable route after 30 seconds, host-admission p99 ≤15 ms, root p75 LCP
      ≤2.5 seconds, no mixed generation, and fail-closed capacity exhaustion.

## 12. Rollout, operations, and final verification

- [ ] 12.1 Run the complete census and quarantine ambiguous Sites, hosts, routes,
      locales, claims, CMS heads, Money rows/readers, callbacks, caches, and
      provider mappings without inference or fabricated history.
- [ ] 12.2 Deploy readers/constraints/receipts/adverse fences before writers;
      shadow manifests/qualifications/generations; prove old/new compatibility;
      then fence every predecessor writer and enable narrow explicit cohorts.
- [ ] 12.3 Extend the existing strict local CMS E2E runner as the one vertical
      staff/donor/public tracer. Use local HTTP runtimes for protocol/UI truth,
      the published-content reader only for exact generation selection, and
      injected adapters plus receipts for provider/no-effect truth. Add the pure
      command/provider contract suites and real disposable-Postgres matrix; do
      not add another browser harness.
- [ ] 12.4 Pass positive, negative, boundary, authorization, cross-Tenant,
      concurrency, idempotency, failpoint/lost-response, provider ambiguity,
      migration/mixed-version, privacy/retention, accessibility, RTL/CJK, weak-
      network, no-JS, load, and rollback proof.
- [ ] 12.5 Run qualified current Stripe, Vercel, DNS, legal/merchant, security,
      accessibility, and representative staff/donor evidence for the exact
      launch account/deployment topology. Keep unproved modes dark.
- [ ] 12.6 Add PII-safe correlated metrics, alerts, receipts, reconciliation,
      and owner/action runbooks for serving fences, host admission, generation
      coherence, money mismatch, domain ambiguity, Copy privacy/latency, and
      Tenant isolation.
- [ ] 12.7 Keep the PRD, glossary, ADRs, OpenSpec, roadmap, tests, release
      evidence, and published implementation issue congruent. Archive this
      change only after implementation is accepted repository reality.
